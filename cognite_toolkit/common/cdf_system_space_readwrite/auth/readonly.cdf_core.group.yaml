name: 'gp_cdf_core_read_only'
sourceId: '{{readonly_source_id}}'
metadata:
  origin: 'cdf-project-templates'
capabilities:
  - dataModelsAcl:
      actions:
        - READ
      scope:
        spaceIdScope: {
            spaceIds: [
              'cdf_core',
            ]
        }
