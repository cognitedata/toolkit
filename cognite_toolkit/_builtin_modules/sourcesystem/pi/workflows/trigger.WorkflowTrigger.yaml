externalId: pi_population_{{ sourceId }}
triggerRule:
  triggerType: schedule
  cronExpression: "0 4 * * MON"
workflowExternalId: {{ workflow }}
workflowVersion: v1
authentication:
  clientId: ${AVEVA_PI_SPRINGFIELD_CLIENT_ID}
  clientSecret: ${AVEVA_PI_SPRINGFIELD_CLIENT_SECRET}
