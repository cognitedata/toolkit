environment:
  name: dev
  project: some-project
  type: dev
  selected:
    - modules/

variables:
  modules:
    a_module:
      ds_timeseries: ds_timeseries
      source: src
      location: here
      space: sp_graphql_space
    another_module:
      space: sp_my_space
      source: src2
      ds_something: ds_something
      dict_variable: empty
      a_key: some_value
      a_value: 42
