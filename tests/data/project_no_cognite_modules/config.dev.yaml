environment:
  name: dev
  project: some-project
  type: dev
  selected:
    - modules/

variables:
  modules:
    a_module:
      ds_timeseries: ds_timeseries
      source: src
      location: here
    another_module:
      space: sp_my_space
