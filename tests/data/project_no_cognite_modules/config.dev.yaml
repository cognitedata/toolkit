environment:
  name: dev
  project: some-project
  type: dev
  selected_modules_and_packages:
    - modules/a_module
    - modules/another_module

variables:
  modules:
    a_module:
      ds_timeseries: ds_timeseries
      source: src
      location: here
    another_module:
      space: sp_my_space
